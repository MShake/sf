{% extends 'AppBundle::layout.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('bundles/app/css/admin.css') }}" type="text/css" media="all" />
{% endblock %}

{% block body %}
    
    {% include 'AppBundle::menu.html.twig' %}
    
    <h1>Update group</h1>

    <form action="{{ path('admin-group-edit', {id:group.id}) }}" method="post">
        <label>Name : </label>
        {{ form_widget(form.name) }}<br/><br/>
        <input type="submit" value="Update" />
        {{ form_rest(form) }}
    </form>
{% endblock %}
